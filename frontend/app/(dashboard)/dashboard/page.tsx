// Implementado na FASE 9 (Dashboard): cards de indicadores (orçamentos,
// serviços, faturamento) + gráficos. Depende da FASE 3 (rota protegida) e das
// FASES 4-8 (dados reais de clientes, serviços, orçamentos e ordens de serviço).
export default function DashboardPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-text-muted">Dashboard — implementado na FASE 9.</p>
    </main>
  );
}
